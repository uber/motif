plugins {
    id 'net.ltgt.apt-idea' version "0.15"
}

apply plugin: 'java'

sourceCompatibility = 1.8

dependencies {
    annotationProcessor project(':it-compiler')
    compile project(':lib')
    compile 'com.google.truth:truth:0.42'
}