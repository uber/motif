plugins {
    id 'org.jetbrains.kotlin.jvm'
    id 'com.vanniktech.maven.publish'
}

kotlin {
    jvmToolchain(11)
}

dependencies {
    api project(':models')

    implementation deps.kotlin.stdlib
}
