package com.uber.motif.sample.app.photoactions;

import android.content.Context;
import android.support.annotation.Nullable;
import android.util.AttributeSet;
import android.widget.LinearLayout;

public class PhotoActionsView extends LinearLayout {

    public PhotoActionsView(Context context) {
        super(context);
    }

    public PhotoActionsView(Context context, @Nullable AttributeSet attrs) {
        super(context, attrs);
    }

    public PhotoActionsView(Context context, @Nullable AttributeSet attrs, int defStyleAttr) {
        super(context, attrs, defStyleAttr);
    }
}
