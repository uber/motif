apply plugin: 'java'
apply plugin: 'application'

mainClassName = "com.uber.motif.sample.Main"

sourceCompatibility = 1.8

dependencies {
    annotationProcessor project(':compiler')

    compile deps.misc.dagger
    compile project(':lib')
}